###############################################################################
# .env.example (Notebook 最小構成用)
# - Notebook で実際に参照されるキーのみを掲載
# - 文字列の引用符は任意（ここでは付けない）
# - 実値は .env にのみ記載し、このファイルはコミット可
# - 公開環境に実値を push した場合は必ず速やかにローテーション
###############################################################################

# ===== Azure AI Foundry Agent Service =====
# プロジェクト (services.ai.azure.com) のエンドポイント
PROJECT_ENDPOINT=
AZURE_AI_PROJECT_NAME=
# モデルデプロイメント (未指定時は実装側で gpt-4o / gpt-4o-mini 等を既定にする例あり)
MODEL_DEPLOYMENT_NAME=

# ===== Azure OpenAI Service (直接利用) =====
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com/
AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME=
AZURE_OPENAI_CHAT_DEPLOYMENT_NAME=
AZURE_OPENAI_API_VERSION=2024-10-21

# ===== Azure Cognitive Search (RAG サンプル) =====
AZURE_SEARCH_SERVICE_ENDPOINT=https://<your-search>.search.windows.net
AZURE_SEARCH_API_KEY=

# ===== Azure リソースメタ情報 =====
AZURE_SUBSCRIPTION_ID=
AZURE_OPENAI_RESOURCE_GROUP=
AZURE_AI_SERVICE_ENDPOINT=  # Cognitive Services 汎用 Endpoint (一部ノートブックで参照可)

# ===== GitHub Models (GitHub Inference) =====
GITHUB_TOKEN=
GITHUB_PERSONAL_ACCESS_TOKEN=  # 上と同一用途。片方のみ設定で可。将来的には GITHUB_TOKEN に統一推奨。

# ===== 認証 (Entra ID / サービスプリンシパル) =====
# Azure CLI ログインを使う場合は未設定で可。自動化パイプラインで利用する際に追加。
# AZURE_CLIENT_ID=
# AZURE_TENANT_ID=
# AZURE_CLIENT_SECRET=

###############################################################################
# 利用例:
# 1) 最小 (GitHub Models 利用): GITHUB_TOKEN だけ設定
# 2) Azure OpenAI (チャットのみ): AZURE_OPENAI_API_KEY / AZURE_OPENAI_ENDPOINT / AZURE_OPENAI_API_VERSION / AZURE_OPENAI_CHAT_DEPLOYMENT_NAME
# 3) RAG (イベント推薦): 2) + AZURE_SEARCH_SERVICE_ENDPOINT / AZURE_SEARCH_API_KEY
# 4) Agent Service 拡張: 2) または 1) + PROJECT_ENDPOINT (+ MODEL_DEPLOYMENT_NAME 任意)
# 5) Embedding 利用ノートブック: 上記 + AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME
###############################################################################

# 注意:
# - PAT / API Key は決して公開リポジトリへコミットしない
# - 誤って流出した場合は Azure Portal / GitHub で速やかに失効 & 再発行
# - ここに列挙されていない追加変数を使う場合は README / Notebook に理由と使用箇所を明記
###############################################################################

